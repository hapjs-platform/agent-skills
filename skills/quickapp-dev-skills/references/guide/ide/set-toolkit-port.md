# 设置 toolkit 调试端口

> 通过本节你将学会设置 toolkit 调试端口。

1. 通过「**首选项**」「**设置**」菜单，打开设置面板。

2) 搜索 `toolkit port`, 修改端口号。

3) 配置后，需要重新加载窗口才会生效。操作如下：通过快捷键 `Ctrl + Shift + P` (Mac：`Cmd + Shift + P`) 调起「**命令面板**」，输入 `开发人员: 重新加载窗口`。
